# Selftest Cases

Put sample inputs under `sample_inputs/` and run:

```
python tools/selftest_cli.py selftest/cases/sample_inputs/sample.json
```

Goldens are written to `selftest/goldens/<adapter_name>/`.
