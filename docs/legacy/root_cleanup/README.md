# Root Cleanup Archive

这些文件是从项目根目录归档过来的历史文档/备份，保留仅用于追溯，不作为当前入口文档。

归档原因：
- 根目录只保留入口级文件，避免 AI/人工把一次性说明误判为需要持续维护的正式文档。
- 报告、迁移说明、临时计划、备份文件应集中在 `docs/` 下，避免污染仓库入口层。
- 新的临时文档若确有保留价值，也应优先放在 `docs/` 的明确子目录内。
